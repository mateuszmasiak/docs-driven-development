{
  "name": "demo-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "echo 'Dev server running on http://localhost:3000'",
    "test:unit": "echo 'Running unit tests...'",
    "test:e2e": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "vitest": "^1.0.0"
  }
}
